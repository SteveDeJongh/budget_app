<!DOCTYPE html>
<html lang="en-us">
  <head>
    <title>Expense App</title>
    <meta charset="UTF-8">
  </head>
  <body>
    <header>
      <h1>Expense Tracker</h1>
    </header>

    <% if session[:succes]%>
      <div>
        <p><%= session.delete(:success) %></p>
      </div>
    <% end %>

    <main>
      <%== yield %>
    </main>
  </body>
</html>